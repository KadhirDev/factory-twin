global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: "factory-twin-backend"
    static_configs:
      - targets: ["factory-backend:8000"]
    metrics_path: /metrics